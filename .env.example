# ==================== REQUIRED CONFIGURATION ====================
# ⚠️ SECURITY WARNING: Replace ALL placeholder values before deployment!

# Database Connection (used by app/config.py)
DATABASE_URL=postgresql://YOUR_DB_USER:YOUR_SECURE_PASSWORD@localhost:5432/YOUR_DB_NAME

# Docker variables (used by docker-compose.yml)
DB_USER=YOUR_DB_USER
DB_PASSWORD=YOUR_SECURE_PASSWORD
DB_NAME=YOUR_DB_NAME

# ==================== API KEYS ====================
# Google Gemini API (FREE - Get from https://aistudio.google.com/app/apikey)
GOOGLE_API_KEY=your_gemini_api_key_here
GOOGLE_MODEL=gemini-2.5-flash

# ==================== ENVIRONMENT ====================
# Use 'development' for MemoryHuey (no Redis needed) + debug logging
# Use 'production' for RedisHuey (requires Redis) + minimal logging
ENVIRONMENT=production
DEBUG=false

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==================== DEVELOPMENT QUICK START ====================
# For local development, you can use these values (DO NOT USE IN PRODUCTION):
# DATABASE_URL=postgresql://dev_user:dev_password@localhost:5432/dev_triage_db
# DB_USER=dev_user
# DB_PASSWORD=dev_password
# DB_NAME=dev_triage_db
# ENVIRONMENT=development
# DEBUG=true
